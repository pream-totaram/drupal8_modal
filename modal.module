<?php

function modal_page_attachments(&$vars) {
      $vars['#attached']['library'][] = 'modal/jqmodal';
      $vars['#attached']['library'][] = 'modal/modal';
}

function modal_theme($existing, $type, $theme, $path) {
    return [
        'output' => [
            'variables' => ['var' => null]
        ]
    ];
}